# Code Audit Notes

Use this folder for maintainability, correctness, and reproducibility audits of the iRoute codebase.

Stage-1 context:

- The main experiment implementation still lives in `ns-3/scratch/iroute-exp-baselines.cc`
- Runner compatibility wrappers remain in place to avoid workflow breakage
- `GEMINI.md` and `Task.md` remain at the repository root and should be treated as upstream working notes, not canonical audit outputs
