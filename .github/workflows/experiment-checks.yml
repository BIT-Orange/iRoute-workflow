name: Experiment Checks

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  experiment-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run merge gate
        run: bash scripts/workflow.sh merge-gate
